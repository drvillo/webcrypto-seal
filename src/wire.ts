/**
 * WASM-free parse/format helpers for sealed envelopes.
 * Real helpers land in Plan 03 — this stub keeps the `./wire` tsup entry resolvable.
 */
export const WIRE_SUBPATH = '@drvillo/browser-seal-crypto-asymmetric/wire' as const
